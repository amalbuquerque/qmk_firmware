Lejboua nyquist layout
======================

Had to add the `SKIP_GIT=true` to be able to build QMK, after rebasing on top of master.

Do this
-------

1. `make KEYMAP=lejboua` or `make nyquist-rev1-lejboua`
